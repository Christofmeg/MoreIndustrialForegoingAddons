 buildscript {
    repositories {
        jcenter()
        maven { url = "http://files.minecraftforge.net/maven" }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT'
    }
}
apply plugin: 'net.minecraftforge.gradle.forge'

version = "1.12.2-1.1.1"
group = "com.christofmeg.mifa"
archivesBaseName = "mifa"

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = '1.8' 

minecraft {
    version = "1.12.2-14.23.5.2838"
	mappings = "stable_39"
    runDir = "run"
	
    replace "@VERSION@", "1.0"
    replaceIn "MoreIndustrialForegoingAddons.java"
}

repositories {
    // JEI
    maven {
        // location of the maven that hosts JEI files
        name = "Progwml6 maven"
        url = "http://dvs1.progwml6.com/files/maven"
    }
    maven {
        // location of a maven mirror for JEI files, as a fallback
        name = "ModMaven"
        url = "modmaven.k-4u.nl"
    }
}

dependencies {

	//Industrial Foregoing
	compile files("dependencies/industrialforegoing-1.12.2-1.12.13-237.jar")
    compile files("dependencies/tesla-core-lib-1.12.2-1.0.18.jar")
    compile files("dependencies/Forgelin-1.8.4.jar")

    // JEI
	compile files("dependencies/jei_1.12.2-4.16.1.301.jar")
}

processResources {
    // this will ensure that this task is redone when the versions change.
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    // replace stuff in mcmod.info, nothing else
    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        // replace version and mcversion
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    // copy everything else except the mcmod.info
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}
